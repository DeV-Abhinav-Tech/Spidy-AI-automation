{
  "name": "spidy-ai-automation",
  "version": "1.0.0",
  "private": true,
  "description": "Spidy AI Task Automation Assistant - Fullstack FastAPI & React Vite",
  "scripts": {
    "build": "cd frontend && npm install --ignore-scripts && npm run build",
    "start": "cd frontend && npm run preview"
  }
}
